{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Attach to Open Liberty",
            "request": "attach",
            "hostName": "localhost",
            "port": 10542 // 替換為日誌中的 Liberty debug port，如果可以固定port就好
        }
    ]
}